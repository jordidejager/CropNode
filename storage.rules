
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Sta geauthenticeerde gebruikers toe om bestanden te lezen en te schrijven in de 'voorschriften' map.
    match /voorschriften/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
